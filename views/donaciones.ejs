<%- include('partials/header') %>

<div class="min-h-screen bg-gray-50 py-8" x-data="{ selectedMethod: 'nequi' }">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- Header -->
        <div class="text-center mb-12">
            <h1 class="text-4xl font-bold text-gray-900 mb-4">
                ❤️ Apoya el Desarrollo de IAtiva
            </h1>
            <p class="text-xl text-gray-600 max-w-2xl mx-auto">
                Tu donación nos ayuda a seguir desarrollando herramientas financieras gratuitas y de calidad para emprendedores como tú.
            </p>
        </div>

        <!-- Beneficios de Donar -->
        <div class="bg-white rounded-lg shadow-sm p-6 mb-8">
            <h2 class="text-2xl font-semibold text-gray-900 mb-4 text-center">
                🚀 ¿Por qué donar a IAtiva?
            </h2>
            <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
                <div class="text-center">
                    <div class="bg-iativa-blue bg-opacity-10 rounded-full w-16 h-16 flex items-center justify-center mx-auto mb-3">
                        <i class="fas fa-code text-iativa-blue text-2xl"></i>
                    </div>
                    <h3 class="font-semibold text-gray-900 mb-2">Desarrollo Continuo</h3>
                    <p class="text-sm text-gray-600">Nuevas funcionalidades y mejoras constantes</p>
                </div>
                <div class="text-center">
                    <div class="bg-iativa-green bg-opacity-10 rounded-full w-16 h-16 flex items-center justify-center mx-auto mb-3">
                        <i class="fas fa-server text-iativa-green text-2xl"></i>
                    </div>
                    <h3 class="font-semibold text-gray-900 mb-2">Infraestructura</h3>
                    <p class="text-sm text-gray-600">Servidores y servicios para mantener la app funcionando</p>
                </div>
                <div class="text-center">
                    <div class="bg-iativa-gold bg-opacity-10 rounded-full w-16 h-16 flex items-center justify-center mx-auto mb-3">
                        <i class="fas fa-graduation-cap text-iativa-gold text-2xl"></i>
                    </div>
                    <h3 class="font-semibold text-gray-900 mb-2">Educación</h3>
                    <p class="text-sm text-gray-600">Contenido educativo y recursos gratuitos</p>
                </div>
                <div class="text-center">
                    <div class="bg-iativa-purple bg-opacity-10 rounded-full w-16 h-16 flex items-center justify-center mx-auto mb-3">
                        <i class="fas fa-heart text-iativa-purple text-2xl"></i>
                    </div>
                    <h3 class="font-semibold text-gray-900 mb-2">Comunidad</h3>
                    <p class="text-sm text-gray-600">Apoyo y crecimiento de la comunidad emprendedora</p>
                </div>
            </div>
        </div>

        <!-- Métodos de Pago -->
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-8">
            <h2 class="text-2xl font-semibold text-gray-900 mb-6 text-center">
                💳 Métodos de Pago Disponibles
            </h2>
            <div class="grid md:grid-cols-2 gap-6">
                <!-- Nequi -->
                <div class="border-2 rounded-lg p-4 cursor-pointer transition-all duration-200"
                     :class="selectedMethod === 'nequi' ? 'border-purple-500 bg-purple-50' : 'border-gray-200 hover:border-purple-300'"
                     @click="selectedMethod = 'nequi'">
                    <div class="flex items-center justify-center mb-3">
                        <div class="w-12 h-12 bg-purple-600 rounded-lg flex items-center justify-center">
                            <i class="fas fa-mobile-alt text-white text-xl"></i>
                        </div>
                        <span class="ml-3 text-xl font-semibold text-purple-600">Nequi</span>
                    </div>
                    <p class="text-sm text-gray-600 text-center mb-2">
                        Rápido, fácil y directo desde tu celular
                    </p>
                    <p class="text-xs text-center text-purple-600 font-medium">
                        ✨ Recomendado - Sin comisiones adicionales
                    </p>
                </div>

                <!-- MercadoPago -->
                <div class="border-2 rounded-lg p-4 cursor-pointer transition-all duration-200"
                     :class="selectedMethod === 'mercadopago' ? 'border-blue-500 bg-blue-50' : 'border-gray-200 hover:border-blue-300'"
                     @click="selectedMethod = 'mercadopago'">
                    <div class="flex items-center justify-center mb-3">
                        <div class="w-12 h-12 bg-blue-500 rounded-lg flex items-center justify-center">
                            <i class="fas fa-credit-card text-white text-xl"></i>
                        </div>
                        <span class="ml-3 text-xl font-semibold text-blue-600">MercadoPago</span>
                    </div>
                    <p class="text-sm text-gray-600 text-center mb-2">
                        Tarjetas de crédito/débito, PSE y más
                    </p>
                    <p class="text-xs text-center text-blue-600 font-medium">
                        🔒 Pago seguro y protegido
                    </p>
                </div>
            </div>
        </div>

        <!-- Opciones de Donación -->
        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
            <% donaciones.forEach((donacion, index) => { %>
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 text-center hover:shadow-md transition-shadow duration-200">
                <div class="text-3xl mb-3">
                    <% if (index === 0) { %>☕
                    <% } else if (index === 1) { %>🍽️
                    <% } else if (index === 2) { %>🚀
                    <% } else { %>👑<% } %>
                </div>
                <h3 class="text-xl font-semibold text-gray-900 mb-2"><%= donacion.nombre %></h3>
                <p class="text-gray-600 text-sm mb-4"><%= donacion.descripcion %></p>
                <div class="text-2xl font-bold text-iativa-blue mb-4">
                    $<%= donacion.precio.toLocaleString('es-CO') %> COP
                </div>
                <button @click="donate('<%= Object.keys(donaciones)[index] %>', selectedMethod)" 
                        class="w-full bg-iativa-blue text-white py-2 px-4 rounded-lg hover:bg-blue-600 transition-colors duration-200">
                    Donar Ahora
                </button>
            </div>
            <% }); %>
        </div>

        <!-- Donación Personalizada -->
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-8 mb-8">
            <h2 class="text-2xl font-semibold text-gray-900 mb-6 text-center">
                💝 Donación Personalizada
            </h2>
            <div class="max-w-md mx-auto">
                <form id="customDonationForm">
                    <div class="mb-4">
                        <label for="customAmount" class="block text-sm font-medium text-gray-700 mb-2">
                            Monto (COP)
                        </label>
                        <input type="number" 
                               id="customAmount" 
                               name="customAmount"
                               min="1000" 
                               step="1000"
                               placeholder="ej: 25000"
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-iativa-blue focus:border-transparent">
                    </div>
                    <div class="mb-4">
                        <label for="donorName" class="block text-sm font-medium text-gray-700 mb-2">
                            Tu Nombre (opcional)
                        </label>
                        <input type="text" 
                               id="donorName" 
                               name="donorName"
                               placeholder="ej: Juan Pérez"
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-iativa-blue focus:border-transparent">
                    </div>
                    <div class="mb-6">
                        <label for="donorEmail" class="block text-sm font-medium text-gray-700 mb-2">
                            Tu Email *
                        </label>
                        <input type="email" 
                               id="donorEmail" 
                               name="donorEmail"
                               placeholder="tu@email.com"
                               required
                               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-iativa-blue focus:border-transparent">
                    </div>
                    <button type="submit"
                            class="w-full bg-gradient-to-r from-iativa-gold to-iativa-blue text-white py-3 px-4 rounded-lg hover:opacity-90 transition-opacity duration-200 font-semibold">
                        💖 Realizar Donación Personalizada
                    </button>
                    <div class="mt-3 text-sm text-center text-gray-600">
                        Método seleccionado: <span x-text="selectedMethod === 'nequi' ? 'Nequi' : 'MercadoPago'" class="font-semibold"></span>
                    </div>
                </form>
            </div>
        </div>

        <!-- Transparencia -->
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 text-center">
            <h2 class="text-xl font-semibold text-gray-900 mb-4">
                🔍 Transparencia Total
            </h2>
            <p class="text-gray-600 mb-4">
                Todas las donaciones se destinan directamente al desarrollo y mantenimiento de IAtiva.
                Publicamos informes de transparencia regularmente en nuestro blog.
            </p>
            <div class="grid md:grid-cols-2 gap-4">
                <div class="bg-purple-50 rounded-lg p-4">
                    <div class="flex items-center justify-center mb-2">
                        <i class="fas fa-mobile-alt text-purple-600 mr-2"></i>
                        <span class="font-semibold text-purple-600">Nequi</span>
                    </div>
                    <p class="text-xs text-gray-600">
                        Transferencias directas y seguras<br>
                        Sin comisiones adicionales
                    </p>
                </div>
                <div class="bg-blue-50 rounded-lg p-4">
                    <div class="flex items-center justify-center mb-2">
                        <i class="fas fa-credit-card text-blue-600 mr-2"></i>
                        <span class="font-semibold text-blue-600">MercadoPago</span>
                    </div>
                    <p class="text-xs text-gray-600">
                        Procesamiento seguro y protegido<br>
                        Múltiples métodos de pago
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal de Carga -->
<div id="loadingModal" class="fixed inset-0 bg-black bg-opacity-50 hidden items-center justify-center z-50">
    <div class="bg-white rounded-lg p-6 max-w-sm mx-auto text-center">
        <div class="animate-spin rounded-full h-12 w-12 border-b-2 border-iativa-blue mx-auto mb-4"></div>
        <p class="text-gray-700">Procesando tu donación...</p>
        <p class="text-sm text-gray-500 mt-2">Serás redirigido a MercadoPago</p>
    </div>
</div>

<script>
// Función para donar con opciones predefinidas
async function donate(tipo, metodo) {
    const email = prompt('Por favor, ingresa tu email para procesar la donación:');
    if (!email || !email.includes('@')) {
        alert('Por favor, ingresa un email válido');
        return;
    }

    const nombre = prompt('Tu nombre (opcional):') || '';

    await processDonation(tipo, null, email, nombre, metodo);
}

// Función para donación personalizada
document.getElementById('customDonationForm').addEventListener('submit', async function(e) {
    e.preventDefault();

    const amount = parseInt(document.getElementById('customAmount').value);
    const name = document.getElementById('donorName').value;
    const email = document.getElementById('donorEmail').value;

    if (!amount || amount < 1000) {
        alert('El monto mínimo de donación es $1,000 COP');
        return;
    }

    if (!email) {
        alert('El email es requerido');
        return;
    }

    // Obtener método seleccionado de Alpine.js
    const selectedMethod = window.Alpine ?
        document.querySelector('[x-data]').__x.$data.selectedMethod : 'nequi';

    await processDonation('personalizada', amount, email, name, selectedMethod);
});

// Procesar donación
async function processDonation(tipo, monto, email, nombre, metodo) {
    // Si es Nequi, manejar directamente
    if (metodo === 'nequi') {
        processNequiDonation(tipo, monto, email, nombre);
        return;
    }

    // Si es MercadoPago, usar el flujo existente
    const loadingModal = document.getElementById('loadingModal');
    loadingModal.classList.remove('hidden');
    loadingModal.classList.add('flex');

    try {
        const response = await fetch('/api/create-donation', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({
                tipo: tipo,
                monto_personalizado: monto,
                email: email,
                nombre: nombre
            })
        });

        const data = await response.json();

        if (data.success) {
            // Redirigir a MercadoPago
            window.location.href = data.payment_url;
        } else {
            throw new Error(data.error || 'Error procesando la donación');
        }

    } catch (error) {
        console.error('Error:', error);
        alert('Error procesando la donación: ' + error.message);
        loadingModal.classList.add('hidden');
        loadingModal.classList.remove('flex');
    }
}

// Procesar donación Nequi
function processNequiDonation(tipo, monto, email, nombre) {
    // Obtener el monto correcto
    let amount = monto;
    if (!amount) {
        // Si no es personalizada, obtener el monto predefinido
        const donaciones = {
            'cafe': 5000,
            'almuerzo': 15000,
            'impulso': 50000,
            'patrocinio': 100000
        };
        amount = donaciones[tipo] || 5000;
    }

    // Datos de Nequi
    const nequiNumber = '3217439415';
    const message = `Donación IAtiva - ${amount.toLocaleString('es-CO')} COP${nombre ? ` - ${nombre}` : ''}`;

    // Crear URL de Nequi
    const nequiUrl = `nequi://p2p?number=${nequiNumber}&amount=${amount}&message=${encodeURIComponent(message)}`;

    // Mostrar modal con instrucciones
    showNequiModal(amount, nequiNumber, message, nequiUrl);
}

// Mostrar modal de Nequi
function showNequiModal(amount, number, message, nequiUrl) {
    const modal = document.createElement('div');
    modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';
    modal.innerHTML = `
        <div class="bg-white rounded-lg max-w-md w-full p-6">
            <div class="text-center mb-6">
                <div class="w-16 h-16 bg-purple-600 rounded-full flex items-center justify-center mx-auto mb-4">
                    <i class="fas fa-mobile-alt text-white text-2xl"></i>
                </div>
                <h3 class="text-xl font-semibold text-gray-900 mb-2">Donación con Nequi</h3>
                <p class="text-gray-600">Sigue estos pasos para completar tu donación</p>
            </div>

            <div class="space-y-4 mb-6">
                <div class="bg-purple-50 rounded-lg p-4">
                    <p class="text-sm font-medium text-purple-800 mb-2">💰 Monto a donar:</p>
                    <p class="text-2xl font-bold text-purple-600">$${amount.toLocaleString('es-CO')} COP</p>
                </div>

                <div class="bg-gray-50 rounded-lg p-4">
                    <p class="text-sm font-medium text-gray-800 mb-2">📱 Número Nequi:</p>
                    <p class="text-lg font-mono font-bold text-gray-900">${number}</p>
                </div>

                <div class="bg-gray-50 rounded-lg p-4">
                    <p class="text-sm font-medium text-gray-800 mb-2">✍️ Mensaje (opcional):</p>
                    <p class="text-sm text-gray-700">${message}</p>
                </div>
            </div>

            <div class="space-y-3">
                <button onclick="window.open('${nequiUrl}', '_blank')"
                        class="w-full bg-purple-600 text-white py-3 px-4 rounded-lg hover:bg-purple-700 transition-colors duration-200 font-semibold">
                    📱 Abrir App Nequi
                </button>

                <div class="text-center">
                    <p class="text-xs text-gray-500 mb-2">¿No tienes la app instalada?</p>
                    <button onclick="copyToClipboard('${number}')"
                            class="text-purple-600 hover:text-purple-800 text-sm font-medium">
                        📋 Copiar número Nequi
                    </button>
                </div>

                <button onclick="this.closest('.fixed').remove()"
                        class="w-full bg-gray-200 text-gray-800 py-2 px-4 rounded-lg hover:bg-gray-300 transition-colors duration-200">
                    Cerrar
                </button>
            </div>
        </div>
    `;

    document.body.appendChild(modal);

    // Cerrar modal al hacer click fuera
    modal.addEventListener('click', function(e) {
        if (e.target === modal) {
            modal.remove();
        }
    });
}

// Copiar al portapapeles
function copyToClipboard(text) {
    navigator.clipboard.writeText(text).then(function() {
        alert('Número Nequi copiado: ' + text);
    }, function() {
        // Fallback para navegadores sin soporte
        const textArea = document.createElement('textarea');
        textArea.value = text;
        document.body.appendChild(textArea);
        textArea.select();
        document.execCommand('copy');
        document.body.removeChild(textArea);
        alert('Número Nequi copiado: ' + text);
    });
}
</script>

<%- include('partials/footer') %>