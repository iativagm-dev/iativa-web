<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Error' %> - IAtiva</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'iativa-blue': '#2563eb',
                        'iativa-green': '#059669',
                        'iativa-orange': '#ea580c'
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">
    <div class="max-w-md w-full mx-auto p-6">
        <div class="bg-white rounded-lg shadow-lg p-8 text-center">
            <!-- Logo/Icono -->
            <div class="mb-6">
                <div class="mx-auto w-16 h-16 bg-red-100 rounded-full flex items-center justify-center">
                    <i class="fas fa-exclamation-triangle text-red-600 text-2xl"></i>
                </div>
            </div>
            
            <!-- Título -->
            <h1 class="text-2xl font-bold text-gray-900 mb-4">
                <%= title || 'Error' %>
            </h1>
            
            <!-- Mensaje -->
            <p class="text-gray-600 mb-6">
                <%= message || 'Ha ocurrido un error inesperado. Por favor, inténtalo de nuevo.' %>
            </p>
            
            <!-- Botones -->
            <div class="space-y-3">
                <% if (locals.backUrl) { %>
                    <a href="<%= backUrl %>" 
                       class="block w-full bg-iativa-blue text-white py-2 px-4 rounded-lg hover:bg-blue-700 transition-colors">
                        <i class="fas fa-arrow-left mr-2"></i>Volver
                    </a>
                <% } %>
                
                <a href="/" 
                   class="block w-full bg-gray-500 text-white py-2 px-4 rounded-lg hover:bg-gray-600 transition-colors">
                    <i class="fas fa-home mr-2"></i>Ir al Inicio
                </a>
            </div>
            
            <!-- Footer -->
            <div class="mt-8 pt-6 border-t border-gray-200">
                <div class="flex items-center justify-center text-gray-500">
                    <i class="fas fa-brain text-iativa-blue mr-2"></i>
                    <span class="text-sm">IAtiva - Asesor de Costeo</span>
                </div>
            </div>
        </div>
    </div>
</body>
</html>